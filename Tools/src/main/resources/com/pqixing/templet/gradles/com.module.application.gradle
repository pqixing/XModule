apply from :"$gradles/com.module.android.gradle"
android {
    buildTypes {
        //生产测试环境
        outTest {
            initWith release
            matchingFallbacks = ['release']
        }
        //测试环境
        inTest {
            initWith release
            matchingFallbacks = ['release']
        }
    }
}
