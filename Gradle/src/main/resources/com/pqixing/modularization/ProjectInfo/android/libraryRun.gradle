package com.pqixing.modularization.ProjectInfo.android
//依赖库工程独立运行的配置文件
android {
    buildTypes {
        dev {
            initWith debug
            matchingFallbacks = ['debug']
        }
    }
}
dependencies {
    //只在deb类型编译使用的依赖，正式打包不会用到
    devImplementation ""
}