apply plugin: "maven"
// 上传到本地代码库
uploadArchives{
    repositories{
        mavenDeployer{
            repository(url:"xxx"){
                authentication(userName: "xxx", password: "xxx")
            }
            pom.groupId = 'xxx' // 组名
            pom.artifactId = 'xxx' // 插件名
            pom.version = '1.0' // 版本号
        }
    }
}
